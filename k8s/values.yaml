# values.yaml

replicaCount: 1

image:
  repository: ogochukwuozotta/frontend-js
  tag: 
  pullPolicy: IfNotPresent

service:
  name: frontend-service
  type: ClusterIP
  port: 8080

env:
  VITE_REACT_APP_API_URL: "http://quizapp.gabapp.tech/api/questions"

resources: {}
